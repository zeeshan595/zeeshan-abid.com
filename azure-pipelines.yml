trigger:
- master

pool: 'default'


stages:
  - stage: 
    displayName: 'Build & Deploy'
    jobs:
      - job:
        displayName: 'Build & Deploy'
        steps:
          - task: Bash@3
            displayName: "Install Packages"
            inputs:
              targetType: 'inline'
              script: |
                npm run build
          - task: Bash@3
            displayName: "Build Project"
            inputs:
              targetType: 'inline'
              script: |
                npm run build