trigger:
- master

pool: 'default'


stages:
  - stage: 
    displayName: 'Build'
    jobs:
      - job:
        displayName: 'Build'
        steps:
          - task: Bash@3
            displayName: "Install Packages"
            inputs:
              targetType: 'inline'
              script: |
                npm install
          - task: Bash@3
            displayName: "Build Project"
            inputs:
              targetType: 'inline'
              script: |
                npm run build